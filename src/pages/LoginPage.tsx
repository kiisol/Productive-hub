import LoginForm from '@features/auth/LoginForm';

export default function LoginPage() {
    return (
        <div style={{padding: 24}}>
            <h2>Login</h2>
            <LoginForm/>
        </div>
    )
}
